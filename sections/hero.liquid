<section class="hero" id="hero">
  <div class="hero__grain"></div>
  <div class="hero__content">
    <p class="hero__tag reveal">Seamless sculpt · Studio tested</p>
    <h1 class="hero__title" id="heroTitle" aria-label="SCULPT STRONGER"></h1>
    <p class="hero__sub reveal">
      TechActive's Sculpt Collection — a seamless bra and high-rise legging
      in squat-proof navy. Second-skin stretch, zero distractions, from the
      first rep to the last.
    </p>
    <div class="hero__actions reveal">
      <a href="/product.html" class="btn btn--primary magnetic">Shop the Drop</a>
      <a href="/#story" class="btn btn--ghost magnetic">The Ritual</a>
    </div>
  </div>
  <div class="hero__visual reveal">
    <div class="hero__frame">
      <img src="{{ 'hero-full.png' | asset_url }}" alt="TechActive Sculpt Collection seamless set in navy" loading="eager" width="800" height="1200" />
      <span class="hero__chip hero__chip--a">Seamless knit</span>
      <span class="hero__chip hero__chip--b">Sculpt fit</span>
    </div>
    <p class="hero__caption">Sculpt Collection — navy</p>
  </div>
  <a href="#marquee" class="hero__scroll" id="scrollHint">
    <span>Scroll</span>
    <span class="hero__scrollline"></span>
  </a>
</section>

<section class="marquee" id="marquee">
  <div class="marquee__track">
    <div class="marquee__group">
      <span><b>Seamless</b> knit</span><i>✦</i>
      <span>Squat-proof</span><i>✦</i>
      <span>4-way stretch</span><i>✦</i>
      <span>Second-skin fit</span><i>✦</i>
      <span>Sweat-wicking</span><i>✦</i>
      <span>No-chafe seams</span><i>✦</i>
    </div>
    <div class="marquee__group">
      <span><b>Seamless</b> knit</span><i>✦</i>
      <span>Squat-proof</span><i>✦</i>
      <span>4-way stretch</span><i>✦</i>
      <span>Second-skin fit</span><i>✦</i>
      <span>Sweat-wicking</span><i>✦</i>
      <span>No-chafe seams</span><i>✦</i>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 120px 40px;
    background-color: {{ settings.color_body_bg }};
    position: relative;
    overflow: hidden;
  }
  .hero__content {
    max-width: 500px;
    text-align: center;
  }
  .hero__title {
    font-family: {{ settings.header_family }}, serif;
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.2;
    color: {{ settings.color_headings }};
    margin: 16px 0;
  }
  .hero__sub {
    font-family: {{ settings.base_family }}, sans-serif;
    font-size: 1rem;
    color: {{ settings.color_body_text }};
    margin-bottom: 32px;
    line-height: 1.6;
  }
  .hero__visual {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .hero__frame img {
    max-width: 400px;
    height: auto;
    border-radius: 4px;
  }
  .hero__actions {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }
  .btn {
    display: inline-block;
    padding: 14px 32px;
    font-family: {{ settings.base_family }}, sans-serif;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: 2px;
    transition: all 0.3s;
    cursor: pointer;
  }
  .btn--primary {
    background-color: {{ settings.color_primary }};
    color: {{ settings.color_button_text }};
    border-color: {{ settings.color_primary }};
  }
  .btn--primary:hover { opacity: 0.85; }
  .btn--ghost {
    border: 2px solid {{ settings.color_primary }};
    color: {{ settings.color_primary }};
    background: transparent;
  }
  .btn--ghost:hover { background-color: {{ settings.color_primary }}; color: {{ settings.color_button_text }}; }
  .marquee {
    padding: 40px 0;
    background-color: #fff;
    border-top: 1px solid {{ settings.color_border }};
    border-bottom: 1px solid {{ settings.color_border }};
  }
  .marquee__track {
    display: flex;
    animation: marquee 30s linear infinite;
    width: max-content;
  }
  .marquee__group {
    display: flex;
    gap: 48px;
    align-items: center;
    font-family: {{ settings.base_family }}, sans-serif;
    font-size: 0.85rem;
    color: {{ settings.color_body_text }};
    white-space: nowrap;
  }
  .marquee__group i { color: {{ settings.color_primary }}; }
  @keyframes marquee {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
  @media (max-width: 768px) {
    .hero { padding: 80px 20px; }
    .hero__frame img { max-width: 280px; }
  }
</style>
